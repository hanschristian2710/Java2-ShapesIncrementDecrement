/**
 * This interface is to notify the DrawingView to update the display of the
 * DrawingModel
 *
 */
public interface DrawingView {
	// abstract method notify
	public void notify(DrawingModel d);
}
